import fs from 'fs';

async function test() {
  try {
     // I need the token. Let's see if there's a token stored somewhere?
     // No, localStorage is in the browser. I can't read it from Node.
  } catch(e) {
  }
}
